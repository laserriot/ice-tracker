{
  "name": "ice-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "portfolio_tracker.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "preserve": "npm run build",
    "start": "npm run build && node dist/actions/portfolio_tracker.js",
    "refill": "npm run build && node dist/actions/refill.js",
    "takeprofit": "npm run build && node dist/actions/take_profit.js",
    "changeall": "npm run build && node dist/actions/change_all_targets.js",
    "change": "npm run build && node dist/actions/change_target.js",
    "reset": "npm run build && node dist/actions/reset_invested_value.js",
    "resetall": "npm run build && node dist/actions/reset_all_invested_values.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "body-parser": "^1.20.0",
    "helmet": "^5.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.39",
    "concurrently": "^7.2.1",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1"
  }
}
