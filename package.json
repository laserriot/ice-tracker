{
  "name": "ice-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "portfolio_tracker.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "preserve": "npm run build",
    "serve": "cross-env NODE_ENV=development concurrently \"tsc --watch\" \"nodemon -q dist/index.js\"",
    "prestart": "npm run build",
    "start": "node dist/portfolio_tracker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "axios": "^0.27.2",
    "helmet": "^5.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.39",
    "concurrently": "^7.2.1",
    "nodemon": "^2.0.16"
  }
}
